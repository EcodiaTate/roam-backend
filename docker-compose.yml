services:
  backend:
    build: .
    container_name: roam-backend
    environment:
      - CACHE_DB_PATH=/cache/roam_cache.db
      - PORT=8000
    volumes:
      - roam_cache:/cache
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  roam_cache: